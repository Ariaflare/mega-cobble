{ name: 'Eelevate', flags: { breakable: 1 }, onTryHit(target, source, move) { if (target !== source && move.type === 'Ground' && !(move.ignoreImmunity === true || (move.ignoreImmunity && move.ignoreImmunity['Ground']))) { this.add('-immune', target, '[from] ability: Eelevate'); return null; } }, onSourceAfterFaint(length, target, source, effect) { if (effect && effect.effectType === 'Move') { const bestStat = source.getBestStat(true, true); this.boost({ [bestStat]: length }, source); } } }
